//  this is the file where we can change global variables and styles

module.exports = {
  theme: {
    container: {
      center: true,
      padding: '2rem'
    },
    extend: {},
  },
  variants: {},
  plugins: [],
}
